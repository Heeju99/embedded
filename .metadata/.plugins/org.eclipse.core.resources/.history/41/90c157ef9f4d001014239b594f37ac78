#include "SysTick.h"

TIM_TypeDef *hTickTIM;

void SysTick_Init()
{
	hTickTIM = TIMx;
}

uint32_t getTick()
{
	return hTickTIM->CNT;
}

void SysTick_Start()
{
	hTickTIM->CR1 |= (1<<0);
}

void SysTick_Start()
{
	hTickTIM->CR1 &= ~(1<<0);
}
